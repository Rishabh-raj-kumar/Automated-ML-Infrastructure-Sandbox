apiVersion: v1
kind: Service
metadata:
  name: ml-app-service
spec:
  type: NodePort
  selector:
    app: ml-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30001 # Maps traffic to your local machine